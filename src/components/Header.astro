<header class="header">
  <div class="container">
    <div class="header-content">
      <div class="logo">
        <span class="logo-text">Abraham Galue</span>
      </div>
      <nav class="nav-desktop">
        <ul>
          <li><a href="#top">Inicio</a></li>
          <li><a href="#projects">Proyectos</a></li>
          <li><a href="#about">Sobre mí</a></li>
          <li><a href="#contact">Contacto</a></li>
        </ul>
      </nav>
      <div class="header-actions">
        <button class="menu-toggle" id="menu-toggle" aria-label="Abrir menú">
          <svg class="icon" viewBox="0 0 24 24">
            <path d="M4 6h16M4 12h16M4 18h16"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>
</header>

<style>
  .header {
    backdrop-filter: saturate(180%) blur(5px);
    background: var(--darker-blue-translucent);
    box-shadow: inset 0 -1px 0 0 var(--darker-blue);
    height: var(--header-height);
    left: 0;
    position: fixed;
    top: 0;
    -webkit-backdrop-filter: saturate(180%) blur(5px);
    width: 100%;
    z-index: 100;
  }

  .header-content {
    align-items: center;
    display: flex;
    height: 100%;
    justify-content: space-between;
    min-height: var(--header-height);
  }

  .nav-desktop {
    display: none;
  }

  @media screen and (width >= 768px) {
    .nav-desktop {
      display: block;
    }
  }

  .nav-desktop ul {
    align-items: center;
    display: flex;
    gap: 1.5rem;
  }

  .nav-desktop a {
    color: var(--gray-300);
    padding: 0.5rem 0;
    transition: var(--transition-fast);
  }

  .nav-desktop a:hover {
    color: var(--white);
  }

  .header-actions {
    align-items: center;
    display: flex;
    gap: 1rem;
  }

  .menu-toggle {
    align-items: center;
    background-color: transparent;
    border-radius: var(--border-radius-full);
    border: none;
    color: var(--white);
    cursor: pointer;
    display: flex;
    height: 2.5rem;
    justify-content: center;
    transition: var(--transition-fast);
    width: 2.5rem;
  }

  @media screen and (width >= 768px) {
    .menu-toggle {
      display: none;
    }
  }

  .menu-toggle:hover {
    background-color: rgba(255, 255, 255, 0.1);
  }
</style>
